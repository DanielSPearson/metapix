{
  "dependencies": {
    "file-saver": "^2.0.5",
    "jszip": "^3.6.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.21.0",
    "@babel/core": "^7.21.3",
    "@babel/preset-env": "^7.20.2",
    "@babel/register": "^7.21.0",
    "@testing-library/dom": "^9.0.1",
    "copyfiles": "^2.4.1",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0"
  },
  "scripts": {
    "test": "jest --debug",
    "build": "mkdir -p dist/scripts && cp index.html css/*.css dist && cp -R scripts/* dist/scripts && cp node_modules/jszip/dist/jszip.min.js node_modules/file-saver/dist/FileSaver.min.js dist"
  }
}
